<UserControl x:Class="QL_QuanKaraoke_BETA_1.View.TrangChuUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"             
             mc:Ignorable="d"
             d:DesignHeight="800"
             d:DesignWidth="1200"
             Background="#F5F6FA" DataContext="{StaticResource TrangChuVM}">

    <UserControl.Resources>

        <Style x:Key="StatCard" TargetType="Border">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="0,0,16,0"/>
        </Style>

        <Style x:Key="StatNumber" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#111827"/>
            <Setter Property="FontSize" Value="28"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>

        <Style x:Key="StatTitle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#6B7280"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Margin" Value="0,6,0,0"/>
        </Style>

        <Style x:Key="ChartCard" TargetType="Border">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="0,0,16,0"/>
        </Style>

    </UserControl.Resources>

    <Grid Margin="28">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="24"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="28"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="📊 Tổng quan"
                       FontSize="26"
                       FontWeight="Bold"
                       Foreground="#111827"/>

            <TextBlock Text="Xin chào ! Đây là tổng quan hoạt động của bạn"
                       FontSize="14"
                       Foreground="#6B7280"
                       Margin="0,8,0,0"/>
        </StackPanel>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource StatCard}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <Viewbox HorizontalAlignment="Left" MaxHeight="30" MaxWidth="120" Stretch="Uniform">
                            <TextBlock Text="{Binding DoanhThuThang}"
                                       Style="{StaticResource StatNumber}"
                                       Foreground="#2563EB"
                                       HorizontalAlignment="Left"/>
                        </Viewbox>
                        <TextBlock Text="Doanh thu tháng này" Style="{StaticResource StatTitle}"/>
                    </StackPanel>
                    <Border Width="50" Height="50" Background="#EFF6FF" BorderBrush="#BFDBFE" BorderThickness="1" HorizontalAlignment="Right">
                        <TextBlock Text="💰" FontSize="22" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    </Border>
                </Grid>
            </Border>

            <Border Grid.Column="1" Style="{StaticResource StatCard}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="{Binding LuotDatPhong}" Style="{StaticResource StatNumber}" Foreground="#059669"/>
                        <TextBlock Text="Lượt đặt phòng" Style="{StaticResource StatTitle}"/>
                    </StackPanel>
                    <Border Width="50" Height="50" Background="#ECFDF5" BorderBrush="#A7F3D0" BorderThickness="1" HorizontalAlignment="Right">
                        <TextBlock Text="🎤" FontSize="22" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    </Border>
                </Grid>
            </Border>

            <Border Grid.Column="2" Style="{StaticResource StatCard}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="{Binding DichVuBanChay}" Style="{StaticResource StatNumber}" FontSize="22" Foreground="#D97706"/>
                        <TextBlock Text="Dịch vụ bán chạy nhất" Style="{StaticResource StatTitle}"/>
                    </StackPanel>
                    <Border Width="50" Height="50" Background="#FFFBEB" BorderBrush="#FDE68A" BorderThickness="1" HorizontalAlignment="Right">
                        <TextBlock Text="🍟" FontSize="22" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    </Border>
                </Grid>
            </Border>

            <Border Grid.Column="3" Style="{StaticResource StatCard}" Margin="0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="{Binding PhongTrong}" Style="{StaticResource StatNumber}" Foreground="#4F46E5"/>
                        <TextBlock Text="Phòng đang trống" Style="{StaticResource StatTitle}"/>
                    </StackPanel>
                    <Border Width="50" Height="50" Background="#EEF2FF" BorderBrush="#C7D2FE" BorderThickness="1" HorizontalAlignment="Right">
                        <TextBlock Text="🛏" FontSize="22" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    </Border>
                </Grid>
            </Border>
        </Grid>

        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="1.1*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource ChartCard}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="20"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="📈 Thống kê doanh thu" Foreground="#111827" FontSize="20" FontWeight="Bold"/>
                            <TextBlock Text="Doanh thu từ ngày được chọn đến 1 tháng sau" Foreground="#6B7280" Margin="0,4,0,0"/>
                        </StackPanel>

                        <DatePicker Grid.Column="1" Width="150" Height="30" SelectedDate="{Binding NgayThongKe}"/>
                    </Grid>

                    <Border Grid.Row="2" Background="#F9FAFB" BorderBrush="#E5E7EB" BorderThickness="1" Padding="10">
                        <lvc:CartesianChart Series="{Binding SeriesCollection}" LegendLocation="Right">

                            <lvc:CartesianChart.ChartLegend>
                                <lvc:DefaultLegend Foreground="#111827"/>
                            </lvc:CartesianChart.ChartLegend>

                            <lvc:CartesianChart.AxisX>
                                <lvc:Axis Title="Ngày" Labels="{Binding Labels}" Foreground="#374151" FontSize="13">
                                    <lvc:Axis.Separator>
                                        <lvc:Separator Step="1" StrokeThickness="0"/>
                                    </lvc:Axis.Separator>
                                </lvc:Axis>
                            </lvc:CartesianChart.AxisX>

                            <lvc:CartesianChart.AxisY>
                                <lvc:Axis Title="Doanh thu" LabelFormatter="{Binding Formatter}" Foreground="#374151" FontSize="13">
                                    <lvc:Axis.Separator>
                                        <lvc:Separator Stroke="#E5E7EB" StrokeThickness="1"/>
                                    </lvc:Axis.Separator>
                                </lvc:Axis>
                            </lvc:CartesianChart.AxisY>
                        </lvc:CartesianChart>
                    </Border>
                </Grid>
            </Border>

            <Border Grid.Column="1" Style="{StaticResource ChartCard}" Margin="0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="20"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0">
                        <TextBlock Text="👑 Hạng khách hàng" Foreground="#111827" FontSize="20" FontWeight="Bold"/>
                        <TextBlock Text="Thống kê số lượng khách theo từng hạng" Foreground="#6B7280" Margin="0,4,0,0"/>
                    </StackPanel>

                    <Border Grid.Row="2" Background="#F9FAFB" BorderBrush="#E5E7EB" BorderThickness="1" Padding="10">
                        <lvc:PieChart Series="{Binding PieSeriesCollection}" LegendLocation="Right" HoverPushOut="10" InnerRadius="40">
                            <lvc:PieChart.ChartLegend>
                                <lvc:DefaultLegend Foreground="#111827"/>
                            </lvc:PieChart.ChartLegend>
                        </lvc:PieChart>
                    </Border>
                </Grid>
            </Border>

        </Grid>
    </Grid>
</UserControl>