<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:QL_QuanKaraoke_BETA_1.ClassTheme">
    <Style x:Key="PasswordBoxTheme" TargetType="PasswordBox">

        <!-- Màu chữ password -->
        <Setter Property="Foreground" Value="#E6E6E6"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="CaretBrush" Value="#E6E6E6"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Padding" Value="4,0,10,0"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="PasswordBox">
                    <Border Background="#353340"
            CornerRadius="8">

                        <Grid>

                            <ScrollViewer x:Name="PART_ContentHost"
                          VerticalAlignment="Center"
                          Margin="0"/>

                            <!-- Placeholder -->
                            <TextBlock Text="{TemplateBinding Tag}"
                       IsHitTestVisible="False"
                       Foreground="#8A8A8A"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Left"
                       Margin="4,0,0,0"
                       FontSize="14">

                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">

                                        <Setter Property="Visibility" Value="Hidden"/>

                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding RelativeSource={RelativeSource TemplatedParent},
                                                         Path=(local:PasswordHelper.IsEmpty)}"
                                                         Value="True">

                                                <Setter Property="Visibility" Value="Visible"/>

                                            </DataTrigger>
                                        </Style.Triggers>

                                    </Style>
                                </TextBlock.Style>

                            </TextBlock>

                        </Grid>

                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>