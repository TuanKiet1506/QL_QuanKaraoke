<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ===================== MÀU SẮC ===================== -->
    <Color x:Key="BgDark">#272537</Color>
    <Color x:Key="BgPanel">#1E1C2E</Color>
    <Color x:Key="BgHover">#3A3750</Color>
    <Color x:Key="BgSelected">#6C63FF</Color>
    <Color x:Key="BgSelectedHover">#8078FF</Color>
    <Color x:Key="BgToday">#3D3A55</Color>
    <Color x:Key="Border">#3A3750</Color>
    <Color x:Key="TextPrimary">#FFFFFF</Color>
    <Color x:Key="TextSecondary">#9B99B5</Color>
    <Color x:Key="TextDisabled">#4A4862</Color>
    <Color x:Key="AccentPurple">#6C63FF</Color>

    <SolidColorBrush x:Key="BrushBgDark"       Color="{StaticResource BgDark}"/>
    <SolidColorBrush x:Key="BrushBgPanel"      Color="{StaticResource BgPanel}"/>
    <SolidColorBrush x:Key="BrushBgHover"      Color="{StaticResource BgHover}"/>
    <SolidColorBrush x:Key="BrushBgSelected"   Color="{StaticResource BgSelected}"/>
    <SolidColorBrush x:Key="BrushBorder"       Color="{StaticResource Border}"/>
    <SolidColorBrush x:Key="BrushTextPrimary"  Color="{StaticResource TextPrimary}"/>
    <SolidColorBrush x:Key="BrushTextSecondary" Color="{StaticResource TextSecondary}"/>
    <SolidColorBrush x:Key="BrushTextDisabled" Color="{StaticResource TextDisabled}"/>
    <SolidColorBrush x:Key="BrushAccent"       Color="{StaticResource AccentPurple}"/>
    <SolidColorBrush x:Key="BrushToday"        Color="{StaticResource BgToday}"/>

    <!-- ===================== CALENDAR DAY BUTTON ===================== -->
    <Style x:Key="DarkCalendarDayButtonStyle" TargetType="CalendarDayButton">
        <Setter Property="Width"  Value="34"/>
        <Setter Property="Height" Value="34"/>
        <Setter Property="Foreground" Value="{StaticResource BrushTextPrimary}"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="FontFamily" Value="Segoe UI"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment"   Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CalendarDayButton">
                    <Grid>
                        <!-- Vòng tròn nền -->
                        <Ellipse x:Name="PART_Bg"
                                 Width="30" Height="30"
                                 Fill="Transparent"/>
                        <!-- Viền today (ngày hôm nay có viền màu gì) -->
                        <Ellipse x:Name="PART_TodayRing"
                                 Width="30" Height="30"
                                 Stroke="{StaticResource BrushAccent}"
                                 StrokeThickness="1.5"
                                 Visibility="Collapsed"/>
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <!-- Hover -->
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="PART_Bg" Property="Fill"
                                    Value="{StaticResource BrushBgHover}"/>
                        </Trigger>
                        <!-- Selected -->
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="PART_Bg" Property="Fill"
                                    Value="{StaticResource BrushBgSelected}"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                        <!-- Today -->
                        <Trigger Property="IsToday" Value="True">
                            <Setter TargetName="PART_TodayRing" Property="Visibility"
                                    Value="Visible"/>
                            <Setter Property="Foreground"
                                    Value="{StaticResource BrushAccent}"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                        </Trigger>
                        <!-- Blackout / ngoài tháng -->
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground"
                                    Value="{StaticResource BrushTextDisabled}"/>
                        </Trigger>
                        <Trigger Property="CalendarDayButton.IsInactive" Value="True">
                            <Setter Property="Foreground"
                                    Value="{StaticResource BrushTextDisabled}"/>
                        </Trigger>
                        <!-- Selected + Today -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsSelected" Value="True"/>
                                <Condition Property="IsToday"    Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="PART_TodayRing" Property="Visibility"
                                    Value="Collapsed"/>
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ===================== CALENDAR BUTTON (tháng / năm) ===================== -->
    <Style x:Key="DarkCalendarButtonStyle" TargetType="CalendarButton">
        <Setter Property="Width"  Value="60"/>
        <Setter Property="Height" Value="40"/>
        <Setter Property="Foreground" Value="{StaticResource BrushTextPrimary}"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="FontFamily" Value="Segoe UI"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CalendarButton">
                    <Grid>
                        <Rectangle x:Name="PART_Bg" RadiusX="6" RadiusY="6" Fill="Transparent"/>
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="PART_Bg" Property="Fill"
                                    Value="{StaticResource BrushBgHover}"/>
                        </Trigger>
                        <!--<Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="PART_Bg" Property="Fill"
                                    Value="{StaticResource BrushBgSelected}"/>
                        </Trigger>-->
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ===================== CALENDAR ITEM HEADER (T2 T3 ...) ===================== -->
    <Style x:Key="DarkCalendarItemStyle" TargetType="CalendarItem">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CalendarItem">
                    <ControlTemplate.Resources>
                        <!-- DataTemplate cho header thứ trong tuần -->
                        <DataTemplate x:Key="DayHeaderTemplate">
                            <TextBlock Text="{Binding}"
                                       Foreground="{StaticResource BrushTextSecondary}"
                                       FontSize="11"
                                       FontFamily="Segoe UI"
                                       FontWeight="SemiBold"
                                       Width="34" Height="28"
                                       TextAlignment="Center"
                                       VerticalAlignment="Center"/>
                        </DataTemplate>
                    </ControlTemplate.Resources>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition/>
                        </Grid.RowDefinitions>

                        <!-- HEADER: nút điều hướng tháng -->
                        <Grid Grid.Row="0" Margin="8,10,8,6">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <!--Nút prev-->
                            <Button x:Name="PART_PreviousButton"
                                    Grid.Column="0"
                                    Width="28" Height="28"
                                    Cursor="Hand"
                                    Foreground="{StaticResource BrushTextSecondary}"
                                    Background="Transparent"
                                    BorderThickness="0"
                                    Content="&#8249;"
                                    FontSize="20"
                                    VerticalContentAlignment="Center"
                                    HorizontalContentAlignment="Center">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border x:Name="bd" Background="Transparent" CornerRadius="6">
                                                        <ContentPresenter HorizontalAlignment="Center"
                                                                          VerticalAlignment="Center"/>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter TargetName="bd" Property="Background"
                                                                    Value="{StaticResource BrushBgHover}"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </Button.Style>
                            </Button>

                            <!--Tiêu đề tháng/năm-->
                            <Button x:Name="PART_HeaderButton"
                                    Grid.Column="1"
                                    Foreground="{StaticResource BrushTextPrimary}"
                                    Background="Transparent"
                                    BorderThickness="0"
                                    FontSize="13"
                                    FontWeight="SemiBold"
                                    FontFamily="Segoe UI"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Cursor="Hand">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border x:Name="bd" Background="Transparent"
                                                            CornerRadius="6" Padding="8,4">
                                                        <ContentPresenter HorizontalAlignment="Center"
                                                                          VerticalAlignment="Center"/>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter TargetName="bd" Property="Background"
                                                                    Value="{StaticResource BrushBgHover}"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </Button.Style>
                            </Button>

                            <!--Nút next-->
                            <Button x:Name="PART_NextButton"
                                    Grid.Column="2"
                                    Width="28" Height="28"
                                    Cursor="Hand"
                                    Foreground="{StaticResource BrushTextSecondary}"
                                    Background="Transparent"
                                    BorderThickness="0"
                                    Content="&#8250;"
                                    FontSize="20"
                                    VerticalContentAlignment="Center"
                                    HorizontalContentAlignment="Center">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border x:Name="bd" Background="Transparent" CornerRadius="6">
                                                        <ContentPresenter HorizontalAlignment="Center"
                                                                          VerticalAlignment="Center"/>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter TargetName="bd" Property="Background"
                                                                    Value="{StaticResource BrushBgHover}"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </Button.Style>
                            </Button>
                        </Grid>

                        <!-- Đường kẻ phân cách -->
                        <Rectangle Grid.Row="1" Height="1" Margin="10,0"
                                   Fill="{StaticResource BrushBorder}"/>

                        <!-- LƯỚI NGÀY -->
                        <Grid x:Name="PART_MonthView" Grid.Row="2" Margin="8,6,8,8"
                              Visibility="Visible">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition/>
                                <RowDefinition/>
                                <RowDefinition/>
                                <RowDefinition/>
                                <RowDefinition/>
                                <RowDefinition/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition/>
                                <ColumnDefinition/>
                                <ColumnDefinition/>
                                <ColumnDefinition/>
                                <ColumnDefinition/>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>
                        </Grid>

                        <!-- LƯỚI NĂM/THẬP KỶ -->
                        <Grid x:Name="PART_YearView" Grid.Row="2" Margin="8,6,8,8"
                              Visibility="Hidden">
                            <Grid.RowDefinitions>
                                <RowDefinition/>
                                <RowDefinition/>
                                <RowDefinition/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition/>
                                <ColumnDefinition/>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>
                        </Grid>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <DataTrigger Binding="{Binding DisplayMode,
                            RelativeSource={RelativeSource FindAncestor,
                            AncestorType=Calendar}}"
                            Value="Year">
                            <Setter TargetName="PART_MonthView" Property="Visibility" Value="Hidden"/>
                            <Setter TargetName="PART_YearView"  Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding DisplayMode,
                            RelativeSource={RelativeSource FindAncestor,
                            AncestorType=Calendar}}"
                            Value="Decade">
                            <Setter TargetName="PART_MonthView" Property="Visibility" Value="Hidden"/>
                            <Setter TargetName="PART_YearView"  Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ===================== CALENDAR ===================== -->
    <Style x:Key="DarkCalendarStyle" TargetType="Calendar">
        <Setter Property="CalendarDayButtonStyle" Value="{StaticResource DarkCalendarDayButtonStyle}"/>
        <Setter Property="CalendarButtonStyle"    Value="{StaticResource DarkCalendarButtonStyle}"/>
        <Setter Property="CalendarItemStyle"      Value="{StaticResource DarkCalendarItemStyle}"/>
        <Setter Property="Background"             Value="Transparent"/>
        <Setter Property="BorderThickness"        Value="0"/>
        <Setter Property="Foreground"             Value="{StaticResource BrushTextPrimary}"/>
    </Style>

    <!-- ===================== DATEPICKER TEXT BOX ===================== -->
    <Style x:Key="DarkDatePickerTextBoxStyle" TargetType="DatePickerTextBox">
        <Setter Property="Background"   Value="Transparent"/>
        <Setter Property="Foreground"   Value="{StaticResource BrushTextPrimary}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding"      Value="0"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="CaretBrush"   Value="{StaticResource BrushAccent}"/>
        <Setter Property="SelectionBrush" Value="{StaticResource BrushBgSelected}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="DatePickerTextBox">
                    <Grid>
                        <ScrollViewer x:Name="PART_ContentHost"
                                      VerticalAlignment="Center"
                                      Margin="0"/>
                        <!-- Watermark -->
                        <TextBlock x:Name="PART_Watermark"
                                   Text="dd/MM/yyyy"
                                   Foreground="{StaticResource BrushTextDisabled}"
                                   VerticalAlignment="Center"
                                   IsHitTestVisible="False"
                                   Visibility="Collapsed"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="Text" Value="">
                            <Setter TargetName="PART_Watermark" Property="Visibility" Value="Visible"/>
                        </Trigger>
                        <Trigger Property="Text" Value="{x:Null}">
                            <Setter TargetName="PART_Watermark" Property="Visibility" Value="Visible"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ===================== DATEPICKER CHÍNH ===================== -->
    <Style x:Key="DarkDatePickerStyle" TargetType="DatePicker">
        <Setter Property="Background"      Value="#1E1C2E"/>
        <Setter Property="Foreground"      Value="{StaticResource BrushTextPrimary}"/>
        <Setter Property="BorderBrush"     Value="{StaticResource BrushBorder}"/>
        <Setter Property="BorderThickness" Value="1.5"/>
        <Setter Property="FontFamily"      Value="Segoe UI"/>
        <Setter Property="FontSize"        Value="13"/>
        <Setter Property="Padding"         Value="10,0,0,0"/>
        <Setter Property="Height"          Value="40"/>
        <Setter Property="CalendarStyle"   Value="{StaticResource DarkCalendarStyle}"/>
        <Setter Property="IsTodayHighlighted" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="DatePicker">
                    <Border x:Name="PART_Border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="10"
                            SnapsToDevicePixels="True">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition Width="40"/>
                            </Grid.ColumnDefinitions>

                            <!-- TextBox nhập ngày -->
                            <DatePickerTextBox x:Name="PART_TextBox"
                                               Grid.Column="0"
                                               Style="{StaticResource DarkDatePickerTextBoxStyle}"
                                               Foreground="{TemplateBinding Foreground}"
                                               Margin="{TemplateBinding Padding}"
                                               VerticalAlignment="Center"/>

                            <!-- Nút mở lịch -->
                            <Button x:Name="PART_Button"
                                    Grid.Column="1"
                                    Width="36" Height="36"
                                    Cursor="Hand"
                                    Focusable="False"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border x:Name="btnBd"
                                                            Background="Transparent"
                                                            CornerRadius="8">
                                                        <!-- Icon lịch SVG-style bằng Path -->
                                                        <Grid Width="18" Height="18">
                                                            <!-- Thân lịch -->
                                                            <Rectangle RadiusX="2" RadiusY="2"
                                                                       Stroke="{StaticResource BrushTextSecondary}"
                                                                       StrokeThickness="1.5"
                                                                       Fill="Transparent"
                                                                       Margin="0,2,0,0"/>
                                                            <!-- Thanh trên -->
                                                            <Rectangle Height="5" VerticalAlignment="Top"
                                                                       RadiusX="2" RadiusY="2"
                                                                       Fill="{StaticResource BrushAccent}"
                                                                       Margin="0,2,0,0"/>
                                                            <!-- Móc trái -->
                                                            <Rectangle Width="1.5" Height="5"
                                                                       Fill="{StaticResource BrushAccent}"
                                                                       HorizontalAlignment="Left"
                                                                       VerticalAlignment="Top"
                                                                       Margin="4,0,0,0"/>
                                                            <!-- Móc phải -->
                                                            <Rectangle Width="1.5" Height="5"
                                                                       Fill="{StaticResource BrushAccent}"
                                                                       HorizontalAlignment="Right"
                                                                       VerticalAlignment="Top"
                                                                       Margin="0,0,4,0"/>
                                                            <!-- Chấm lưới ngày (3 hàng x 3 cột) -->
                                                            <UniformGrid Rows="2" Columns="3"
                                                                         Margin="2,7,2,2">
                                                                <Ellipse Width="2.5" Height="2.5"
                                                                         Fill="{StaticResource BrushTextSecondary}"
                                                                         HorizontalAlignment="Center"
                                                                         VerticalAlignment="Center"/>
                                                                <Ellipse Width="2.5" Height="2.5"
                                                                         Fill="{StaticResource BrushTextSecondary}"
                                                                         HorizontalAlignment="Center"
                                                                         VerticalAlignment="Center"/>
                                                                <Ellipse Width="2.5" Height="2.5"
                                                                         Fill="{StaticResource BrushTextSecondary}"
                                                                         HorizontalAlignment="Center"
                                                                         VerticalAlignment="Center"/>
                                                                <Ellipse Width="2.5" Height="2.5"
                                                                         Fill="{StaticResource BrushTextSecondary}"
                                                                         HorizontalAlignment="Center"
                                                                         VerticalAlignment="Center"/>
                                                                <Ellipse Width="2.5" Height="2.5"
                                                                         Fill="{StaticResource BrushTextSecondary}"
                                                                         HorizontalAlignment="Center"
                                                                         VerticalAlignment="Center"/>
                                                                <Ellipse Width="2.5" Height="2.5"
                                                                         Fill="{StaticResource BrushTextSecondary}"
                                                                         HorizontalAlignment="Center"
                                                                         VerticalAlignment="Center"/>
                                                            </UniformGrid>
                                                        </Grid>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter TargetName="btnBd" Property="Background"
                                                                    Value="{StaticResource BrushBgHover}"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </Button.Style>
                            </Button>

                            <!-- Popup lịch -->
                            <Popup x:Name="PART_Popup"
                                   AllowsTransparency="True"
                                   Placement="Bottom"
                                   StaysOpen="False"
                                   PlacementTarget="{Binding ElementName=PART_Border}">
                                <Border Background="#1E1C2E"
                                        BorderBrush="{StaticResource BrushBorder}"
                                        BorderThickness="1.5"
                                        CornerRadius="14"
                                        Padding="4"
                                        Margin="0,4,0,0">
                                    <Border.Effect>
                                        <DropShadowEffect Color="#000000" BlurRadius="20"
                                                          ShadowDepth="4" Opacity="0.45"/>
                                    </Border.Effect>
                                    <Calendar x:Name="PART_Calendar"
                                              Style="{StaticResource DarkCalendarStyle}"/>
                                </Border>
                            </Popup>
                        </Grid>
                    </Border>

                    <ControlTemplate.Triggers>
                        <!-- Focus: viền sáng accent -->
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="PART_Border" Property="BorderBrush"
                                    Value="{StaticResource BrushAccent}"/>
                        </Trigger>
                        <Trigger Property="IsKeyboardFocusWithin" Value="True">
                            <Setter TargetName="PART_Border" Property="BorderBrush"
                                    Value="{StaticResource BrushAccent}"/>
                            <Setter TargetName="PART_Border" Property="BorderThickness" Value="1.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
